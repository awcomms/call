on mount, update description
search() // maybe, maybe not
search
    if searching, return
    searching = true

search Button
    disabled if editing

TODO
    persistent toast notification if no internet access
    onmount update error, specifying if no internet access

    edit and search disabled if no internet access